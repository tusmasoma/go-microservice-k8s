apiVersion: v1
kind: Service
metadata:
  name: commerce-gateway-service
spec:
  selector:
    app: commerce-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP # ingress will expose this service to the outside world
